import {TaxSpUser} from './tax-sp-user';

export class TaxSpGroup{
	/**users:TaxSpUser[];
	title:string;*/
	requestType:string;
	description:string;
	/*emails:string;
	 saving these ones for later:
	groupId:string;
	isLoaded:boolean;*/
}